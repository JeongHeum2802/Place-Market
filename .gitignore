# 1. 의존성 모듈 (프론트/백엔드 모두 적용됨)
node_modules/
/node_modules

# 2. 환경변수 파일 (보안상 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# 3. 빌드 결과물 (Frontend)
build/
dist/
.next/
out/

# 4. 로그 파일들
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# 5. OS 시스템 파일
.DS_Store
Thumbs.db

# 6. IDE 설정 파일 (선택사항 - 개인 설정 공유 방지)
.vscode/
.idea/